# Retire the legacy S4 callbacks at their original definition. Empty
# OnSuccess=/OnFailure= in a later drop-in cannot remove dependencies.
# Keep the required shared storage preparation dependency.
[Unit]
Requires=aag-storage-sleep-v2.service
After=aag-storage-sleep-v2.service
